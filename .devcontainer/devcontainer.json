{
  "name": "Lime Medical Development",
  "dockerComposeFile": "docker-compose.yml",
  "service": "app",
  "workspaceFolder": "/workspace",
  "customizations": {
    "vscode": {
      "extensions": [
        "dbaeumer.vscode-eslint",
        "esbenp.prettier-vscode",
        "ms-azuretools.vscode-docker",
        "ms-vscode.vscode-typescript-tslint-plugin"
      ]
    }
  },
  "forwardPorts": [3000, 5173, 5432, 9000, 9001],
  "postCreateCommand": "npm install -g concurrently && cd server && npm install && cd ../client && npm install && cd .. && concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
  "remoteUser": "root"
} 